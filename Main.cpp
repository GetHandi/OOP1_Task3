#include <iostream>
#include <cassert>
#include "Time.h"

using namespace std;

int main()
{
	/*
	//assert();
	Time a(0,0,1);
	Time b;
	
	a.DiffTime(2, 5, 6);
	a.PrintTime();
	*/





	//записаь объекта в файл
	/*
	Time c(23, 15, 48);
	c.PrintTime();
	cout << endl;
	Time d;
	Time e(15, 15, 15);
	string path = "TimeData.txt";

	c.toFile(path);
	d.toFile(path);
	e.toFile(path);
	
	Time::fromFile(path);
*/
	
	
	//работа с массивом
	/*int SIZE = 15;
	Time* arr = new Time[SIZE];
	Time::TimeArr(arr, SIZE);
	Time::TimeArrPrint(arr, SIZE);
	delete[] arr;*/
	
	
	
	return 0;
}